<div class="container" style="margin-top: 12%;">
  <h2 style="text-align: center"> Report After Completed Canvassing</h2>

  <%= form_tag report_submit_home_path, method: :post do %>

      <div class="field">
        <%= label_tag :notes %><br />
        <%= text_field_tag :notes %>
      </div>

      <div class="actions">
        <%= f.submit "Submit", class: 'btn-primary' %>
      </div>
  <% end %>
</div>
